import {test} from "./fixtures/account.fixture"

test('TC01', async({account}) => {
    console.log(account);
})

test('TC02', async({account}) => {
    console.log(account);
})

test('TC03', async({account}) => {
    console.log(account);
})